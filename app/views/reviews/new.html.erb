<h1>We are in reviews</h1>

<%= simple_form_for [@restaurant, @review] do |f| %>
     <%= f.input :content %>
     <%= f.input :rating, collection: [0,1,2,3,4,5]%>
     <%= f.submit "Add a New Review", class: "btn btn-warning" %>
<% end %>
